{
  "name": "scheduler-service",
  "version": "1.0.0",
  "private": true,
  "description": "Recurring task scheduler - runs as K8s CronJob",
  "scripts": {
    "prebuild": "node -e \"require('fs').cpSync('../../src/generated', './src/generated', {recursive:true})\"",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "cron-parser": "^4.9.0"
  },
  "devDependencies": {
    "prisma": "^7.8.0",
    "typescript": "^5.0.0",
    "@types/node": "^22.0.0"
  }
}
